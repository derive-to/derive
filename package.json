{
  "name": "dock.build",
  "private": true,
  "packageManager": "pnpm@10.26.1",
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3"],
    "overrides": {
      "esbuild": ">=0.25.0"
    }
  },
  "scripts": {
    "dev": "pnpm --filter @dock/server dev",
    "start": "pnpm --filter @dock/server start",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
